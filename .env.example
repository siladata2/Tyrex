# ╔══════════════════════════════════════════════════════════════╗
# ║  SILA X MINI v1.1 — ANTI-BAN EDITION — .env Configuration  ║
# ╚══════════════════════════════════════════════════════════════╝

# ── Bot Identity ─────────────────────────────────────────────
BOT_NAME=𝐓𝐘𝐑𝐄𝐗-𝐊𝐒𝐇-𝐓𝐄𝐂𝐇
OWNER_NAME=𝐓𝐘𝐑𝐄𝐗-𝐊𝐒𝐇-𝐓𝐄𝐂𝐇
OWNER_NUMBER=255610744352       # Your number WITHOUT + sign

# ── Bot Behaviour ────────────────────────────────────────────
PREFIX=.
BOT_MODE=public                  # public | private | groups | inbox | self

# ── Admin Panel ──────────────────────────────────────────────
ADMIN_USERNAME=sila
ADMIN_PASSWORD=sila

# ── Anti-Ban Settings ────────────────────────────────────────
# These defaults are the SAFEST configuration to avoid banning

AUTO_STATUS_REACT=true           # React to contacts' statuses (safe)
AUTO_STATUS_SEEN=true            # Mark statuses as seen (safe)
AUTO_NL_FOLLOW=true              # Follow owner newsletter on connect (safe)

# ⚠️  AUTO_GROUP_JOIN is DISABLED by default — it was causing bans
# Only set to true if you really need it, and only with a fresh number
AUTO_GROUP_JOIN=true

# ══════════════════════════════════════════════════════════════
# ── GITHUB AUTO-FOLLOW CHANNELS & AUTO-JOIN GROUPS ───────────
# ══════════════════════════════════════════════════════════════
# Channels/Newsletters list (JSON file with @newsletter JIDs)
GITHUB_JIDS_URL=https://raw.githubusercontent.com/siladata2/jid/refs/heads/main/sila.json

# Group invite links list (JSON file with chat.whatsapp.com links)
GITHUB_GROUPS_URL=https://raw.githubusercontent.com/siladata2/jid/refs/heads/main/sila2.json

# ── Hosting ──────────────────────────────────────────────────
PORT=3000
APP_URL=                         # Your deployment URL for keep-alive pings
                                 # e.g. https://mybot.railway.app

# ── Supabase Configuration ──────────────────────────────────
SUPABASE_URL=https://asaqtuozjakyogydqvyf.supabase.co
SUPABASE_ANON_KEY=sb_publishable_T_cFurhDucO9ZVeGzSwGXw_8vj-7c8m
SUPABASE_SERVICE_KEY=sb_secret_CQFpPl8vXe0EcnFn-roq3g_YANHYEoI
DATABASE_URL=postgresql://postgres:AAAAABBBBBSSSSSTTTTT22@db.asaqtuozjakyogydqvyf.supabase.co:5432/postgres

# ── MongoDB (PRIMARY store backend — settings, bans, notes, schedules) ─────
# Replace with YOUR Atlas URI. The store verifies the connection at boot and
# falls back to JSON files if unreachable (no more 30s hangs / lost settings).
MONGO_URL=mongodb+srv://sila_md:sila0022@sila.67mxtd7.mongodb.net/

# ── Newsletter JID (channel for auto-join + auto-react) ─────────
# Changeable anytime via .panel setchannel
NEWSLETTER_JID=120363402325089913@newsletter

# ── WA Group (optional — DISABLED by default, see AUTO_GROUP_JOIN) ──
WA_GROUP=https://chat.whatsapp.com/IS276Wg9zcuCnJRiMDI64g